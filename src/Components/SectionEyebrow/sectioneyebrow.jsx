import React from "react";

export default function SectionEyebrow({ children }) {
  return <p className="font-mono-custom text-xs sm:text-sm text-[var(--accent)] mb-3 tracking-wide">{children}</p>;
}
